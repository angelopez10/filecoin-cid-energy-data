# Filecoin CID Geographic and energy Data app 

## Run it locally 
make sure you have node installed and run `npm i && npm start` , app will be started at [http://localhost:3000](http://localhost:3000)