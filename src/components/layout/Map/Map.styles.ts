import { MapContainer } from "react-leaflet";
import styled from "styled-components";

export const MapWrapper = styled(MapContainer)`
  width: 80%;
  margin: 2em auto;
  height: 70vh;
`;

export const PopUpTitle = styled.h3``;

export const PopUpParragraph = styled.p``;
